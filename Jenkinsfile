pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Test Stage') {
            steps {
                echo "Pipeline is running correctly 🚀"
            }
        }
    }
}
