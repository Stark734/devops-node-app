pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Test Pipeline') {
            steps {
                echo "Pipeline is working correctly 🚀"
            }
        }
    }
}